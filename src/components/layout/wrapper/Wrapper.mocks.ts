import { IWrapper } from '@/layout/wrapper/Wrapper';

const base: IWrapper = {
  title: 'Layout Tile',
  description: 'Some description on Layout',
  type: 'website',
};

export const mockBaseWrapperProps = {
  base,
};
