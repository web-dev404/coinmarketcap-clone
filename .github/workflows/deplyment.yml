name: Deploy Coin-market
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: get repository code
        uses: actions/checkout@v3
      - name: install yarn
        run: npm install --global yarn
      - name: install dependencies
        run: yarn install --frozen-lockfile
